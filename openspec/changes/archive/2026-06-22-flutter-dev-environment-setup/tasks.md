# Tasks: Flutter Dev Environment Setup

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Total estimated changed lines | 260–340 |
| 400-line budget risk | Low |
| Chained PRs recommended | No |
| Chain strategy | pending |
| Delivery strategy | ask-always |

Decision needed before apply: Yes
Chained PRs recommended: No
Chain strategy: pending
400-line budget risk: Low

**Reasoning**: ~260–340 lines, mostly documentation (markdown + json). Well under 400-line budget. Single PR is safe. `ask-always` strategy means orchestrator asks before apply regardless.

## Phase 1: Foundation / Infrastructure

### 1.1 Install Flutter SDK 3.44.2
- [x] Download `flutter_windows_3.44.2-stable.zip` from `storage.googleapis.com/flutter_infra_release/releases/stable/windows/`
- [x] Extract to `D:\flutter\flutter`
- [x] Add `D:\flutter\flutter\bin` to user PATH
- [x] Verify: `flutter --version` exits 0, reports `3.44.x` stable
- **Done when**: `flutter --version` exits 0
- **Files touched**: `D:\flutter\flutter\`, user PATH
- **Est. lines**: ~5

### 1.2 Install JDK 21 + Android command-line tools
- [x] Install JDK 21 (Eclipse Temurin), set `JAVA_HOME`, add `bin\` to PATH
- [x] Download Android cmdline-tools, extract to `D:\Android\Sdk`
- [x] Run `sdkmanager "platforms;android-35" "build-tools;35.0.0"`
- [x] Accept licenses: `flutter doctor --android-licenses`
- [x] Verify: `flutter doctor` shows no `[X]` for Android toolchain
- **Done when**: `flutter doctor` exits 0, no blocking issues
- **Files touched**: `JAVA_HOME`, `ANDROID_HOME`, PATH env vars
- **Est. lines**: ~15
- **Depends on**: 1.1 (Flutter needed for `flutter doctor`)

### 1.3 Resolve .flutter submodule
- [x] Run `git submodule update --init`
- [x] If fails, document manual clone workaround in docs
- [x] Verify `.flutter/bin/` has content
- **Done when**: `.flutter` populated OR fallback documented
- **Files touched**: `.flutter/`
- **Est. lines**: ~3
- **Depends on**: none

## Phase 2: Core Setup & Verification

### 2.1 Run flutter pub get + resolve custom forks
- [x] Run `flutter pub get` from project root
- [x] If fork resolution fails, pin SHAs in `pubspec_overrides.yaml`
- [x] Commit updated `pubspec.lock`
- **Done when**: `flutter pub get` exits 0, all 4 git forks in pub cache
- **Files touched**: `pubspec.lock`, maybe `pubspec_overrides.yaml`
- **Est. lines**: ~5
- **Depends on**: 1.1, 1.3

### 2.2 Run flutter analyze and capture baseline
- [x] Run `flutter analyze` — target: 0 errors
- [x] Document pre-existing warnings/infos (non-blocking)
- **Done when**: `flutter analyze` reports 0 errors
- **Files touched**: baseline doc (optional)
- **Est. lines**: ~2
- **Depends on**: 2.1

### 2.3 Run flutter build apk --debug
- [x] Run `flutter build apk --debug`
- [x] Verify artifact at `build/app/outputs/flutter-apk/app-debug.apk`
- [x] Document any NDK/Gradle/signing issues found
- **Done when**: APK exists at expected path, exit 0
- **Files touched**: `build/` (generated, uncommitted)
- **Est. lines**: ~5
- **Depends on**: 2.2

## Phase 3: Documentation & Configuration

### 3.1 Write docs/setup.md
- [x] Prerequisites section (git, JDK 17, Android SDK)
- [x] Flutter install section (download + extract + PATH)
- [x] Android SDK config section (cmdline-tools, licenses)
- [x] Clone + submodule section
- [x] Build section (pub get → analyze → build apk)
- [x] Troubleshoot section (common failures from design's edge-case table)
- **Done when**: New contributor can reproduce build from clean machine using only this doc
- **Files touched**: `docs/setup.md`
- **Est. lines**: ~150–200
- **Depends on**: 2.3

### 3.2 Write CONTRIBUTING.md + update README.md
- [x] Create `CONTRIBUTING.md` with: pointer to `docs/setup.md`, contribution workflow, Conventional Commits recommendation
- [x] Add "Getting Started" section to `README.md` linking to `CONTRIBUTING.md`
- **Done when**: Both files exist, cross-reference each other
- **Files touched**: `CONTRIBUTING.md`, `README.md`
- **Est. lines**: ~50–80
- **Depends on**: 3.1

### 3.3 Add .vscode/settings.json
- [x] Create `.vscode/settings.json` with Flutter SDK path, Dart analyzer config, format-on-save
- [x] Add recommended VS Code extensions for Dart/Flutter
- **Done when**: VS Code shows Flutter tooling active on project open
- **Files touched**: `.vscode/settings.json`
- **Est. lines**: ~20
- **Depends on**: 1.1 (SDK path known)

## Edge Cases & Notes

- Flutter download is ~1.5GB — document size up front in setup doc
- Android NDK may need manual install if `sdkmanager` doesn't pull it — verify during 2.3
- All 4 custom fork SHAs must be reachable — if not, document per-fork fallback
- `local.properties` auto-generated by `flutter pub get` — no manual creation needed
- Windows long-path errors: document `git config core.longpaths true`
